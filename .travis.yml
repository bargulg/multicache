language: python
python:
  - "3.4"
  - "3.5"
  - "3.6"
  - "3.7-dev" # 3.7 development branch
# command to install dependencies
install: "pip install pytest coveralls python-coveralls"
# command to run tests
script: coverage run --branch py.test tests/
after_success:
    - coveralls
